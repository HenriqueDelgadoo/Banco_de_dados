create database pizzaria; -- cadastro de base de dados

use pizzaria; -- Especifica a base de dados que irá ultilizar

create table usuario (
id_usuario int primary key
); -- cria a tabela chamada aluno, com a id_usuario como chave primária 
create table funcionario (
id_funcionario int primary key
); -- cria a tabela funcionario com o id_funcionario  como chave primária 
create table pizza (
id_pizza int primary key
); -- cria a tabela curso com a chave primária de id_pizza