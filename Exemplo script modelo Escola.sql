create database modelo; -- cadastro de base de dados

use modelo; -- Especifica a base de dados que irá ultilizar

create table aluno (
id_aluno int primary key
); -- cria a tabela chamada aluno, com a id_aluno como chave primária 
create table funcionario (
id_funcionario int primary key
); -- cria a tabela funcionario com o id_funcionario como chave primária 
create table Curso (
id_Curso int primary key
); -- cria a tabela curso com a chave primária de id_curso